# Expo / React Native packages have peer dep declarations that lag behind
# their actual supported versions. legacy-peer-deps prevents spurious
# ERESOLVE failures while still resolving the correct versions.
legacy-peer-deps=true
